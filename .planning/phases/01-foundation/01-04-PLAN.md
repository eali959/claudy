---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Claudy/Claudy/ChatView.swift
  - Claudy/Claudy/ChatMessageBubble.swift
  - Claudy/Claudy/ChatInputBar.swift
  - Claudy/Claudy/ChatHeaderBar.swift
  - Claudy/Claudy/LocalChatResponder.swift
  - Claudy/Claudy/LocalChatResponder+Coding.swift
  - Claudy/Claudy/LocalChatResponder+Focus.swift
  - Claudy/Claudy/LocalChatResponder+Personality.swift
  - Claudy/Claudy/LocalChatResponder+Greetings.swift
  - Claudy/Claudy/LocalChatResponder+Emotional.swift
  - Claudy/Claudy/LocalChatResponder+Technical.swift
autonomous: true
requirements:
  - DEBT-10
  - DEBT-11

must_haves:
  truths:
    - "ChatView.swift is under 200 lines after extraction — outer container and scroll coordination only"
    - "LocalChatResponder.swift is under 400 lines — respond() matcher and helpers only"
    - "Chat UI renders identically — message bubbles, input bar, header all present"
    - "LocalChatResponder still responds correctly — all intent categories intact in extension files"
  artifacts:
    - path: "Claudy/Claudy/ChatView.swift"
      provides: "Thin chat container with scroll coordination"
      min_lines: 50
    - path: "Claudy/Claudy/ChatMessageBubble.swift"
      provides: "User + assistant bubble rendering"
    - path: "Claudy/Claudy/ChatInputBar.swift"
      provides: "Text field + send button"
    - path: "Claudy/Claudy/ChatHeaderBar.swift"
      provides: "Mode toggle + clear history button"
    - path: "Claudy/Claudy/LocalChatResponder.swift"
      provides: "respond() intent matcher, kept under 400 lines"
    - path: "Claudy/Claudy/LocalChatResponder+Coding.swift"
      provides: "Coding/debugging/build response pools"
    - path: "Claudy/Claudy/LocalChatResponder+Focus.swift"
      provides: "Pomodoro/focus/productivity/break response pools"
    - path: "Claudy/Claudy/LocalChatResponder+Personality.swift"
      provides: "Arrival messages and personality-specific greeting pools"
    - path: "Claudy/Claudy/LocalChatResponder+Greetings.swift"
      provides: "Hello/goodbye and general greeting pools"
    - path: "Claudy/Claudy/LocalChatResponder+Emotional.swift"
      provides: "Motivation/stress/celebrate response pools"
    - path: "Claudy/Claudy/LocalChatResponder+Technical.swift"
      provides: "Git/API/database/deploy/terminal response pools"
  key_links:
    - from: "ChatView.swift"
      to: "ChatMessageBubble.swift"
      via: "ForEach over messages, ChatMessageBubble(message:) call"
      pattern: "ChatMessageBubble"
    - from: "LocalChatResponder.swift respond()"
      to: "Extension files"
      via: "Response pool arrays defined in extensions, accessed by respond()"
      pattern: "LocalChatResponder\\+"
---

<objective>
Extract ChatView.swift (740 lines) into a container plus three sub-view files, and split LocalChatResponder.swift (1,790 lines) into the core matcher plus six extension files by response category.

Purpose: Two of the largest files in the codebase. Both are targets for Phase 5 and Phase 6 features. Splitting now prevents future merge conflicts and makes individual components testable.
Output: ChatView.swift reduced to ~150 lines; 3 new Chat sub-view files. LocalChatResponder.swift reduced to ~350 lines; 6 new extension files.
</objective>

<execution_context>
@/Users/eali/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/PROJECT.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/STATE.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ChatView into sub-view components (DEBT-10)</name>
  <files>
    Claudy/Claudy/ChatView.swift
    Claudy/Claudy/ChatMessageBubble.swift
    Claudy/Claudy/ChatInputBar.swift
    Claudy/Claudy/ChatHeaderBar.swift
  </files>
  <action>
Read the full ChatView.swift (740 lines) first. Identify the three natural extraction boundaries:

1. **ChatMessageBubble** — the bubble rendering for a single message (user vs assistant styling, text content, timestamp if any). This renders one `ChatMessage` and is called inside a `ForEach`.

2. **ChatInputBar** — the text field, send button, and any input-related state. Receives `@Binding var inputText: String` and a `onSend: () -> Void` closure.

3. **ChatHeaderBar** — the top bar with mode toggle (API vs Companion), clear history button, and any title/status display.

**Pattern for extraction:**
```swift
// ChatMessageBubble.swift
import SwiftUI

struct ChatMessageBubble: View {
    let message: ChatMessage
    // Any other display-only properties

    var body: some View {
        // Exact bubble content moved here
    }
}
```

**ChatView.swift after extraction:**
```swift
import SwiftUI

struct ChatView: View {
    @ObservedObject var viewModel: ChatViewModel  // or @Bindable if @Observable

    var body: some View {
        VStack(spacing: 0) {
            ChatHeaderBar(/* params */)
            ScrollViewReader { proxy in
                ScrollView {
                    ForEach(viewModel.messages) { message in
                        ChatMessageBubble(message: message)
                    }
                }
            }
            ChatInputBar(/* params */)
        }
        // Window-level modifiers stay here
    }
}
```

**Rules:**
- All scroll coordination logic (ScrollViewReader, scroll-to-bottom) stays in ChatView.swift
- All `@ObservedObject` / `@Bindable` ViewModel references stay in ChatView.swift
- Sub-views receive only what they need to render — no ViewModel references inside sub-views
- Do not change any visual styling, colors, fonts, or spacing
- The TypingIndicatorView inclusion (if present) stays in ChatView.swift at the scroll level

After extraction, ChatView.swift must be under 200 lines.
  </action>
  <verify>
    <automated>wc -l "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/ChatView.swift" && ls "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/Chat"*.swift</automated>
    <manual>ChatView.swift line count is under 200. ChatMessageBubble.swift, ChatInputBar.swift, ChatHeaderBar.swift all exist.</manual>
  </verify>
  <done>ChatView.swift is under 200 lines. Three sub-view files created. Chat UI composition preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Split LocalChatResponder into extension files by category (DEBT-11)</name>
  <files>
    Claudy/Claudy/LocalChatResponder.swift
    Claudy/Claudy/LocalChatResponder+Coding.swift
    Claudy/Claudy/LocalChatResponder+Focus.swift
    Claudy/Claudy/LocalChatResponder+Personality.swift
    Claudy/Claudy/LocalChatResponder+Greetings.swift
    Claudy/Claudy/LocalChatResponder+Emotional.swift
    Claudy/Claudy/LocalChatResponder+Technical.swift
  </files>
  <action>
Read the full LocalChatResponder.swift (1,790 lines) first. The file contains a `respond()` method with intent-matching logic, interleaved with large private String array response pools.

**What stays in LocalChatResponder.swift:**
- The `respond(_ input: String, personality: PersonalityMode, ...) -> String` method (the intent matcher)
- Any helper methods called by `respond()`
- Any type definitions (enums, structs) used across the file
- The class/struct declaration itself

**What moves to extension files:**
Move the private String array response pools to extension files. Each extension file holds pools for one thematic category.

**Extension file pattern:**
```swift
// LocalChatResponder+Coding.swift
import Foundation

extension LocalChatResponder {
    // All coding/debugging/build-related response pool arrays
    var codingResponses: [String] { [...] }
    var debuggingResponses: [String] { [...] }
    var buildSuccessResponses: [String] { [...] }
    // etc.
}
```

**Extension file assignments (align to // MARK: sections in the file):**
- `LocalChatResponder+Coding.swift` — coding, debugging, build success/fail response pools
- `LocalChatResponder+Focus.swift` — pomodoro, focus, productivity, break response pools
- `LocalChatResponder+Personality.swift` — arrival messages, personality-specific greeting pools
- `LocalChatResponder+Greetings.swift` — hello/goodbye, general greeting pools
- `LocalChatResponder+Emotional.swift` — motivation, stress, celebration, mood response pools
- `LocalChatResponder+Technical.swift` — git, API, database, deploy, terminal response pools

**Rules:**
- Do NOT change the logic in `respond()` — only move the pool arrays
- If a pool array is accessed via `self.codingResponses` in `respond()`, the name must match exactly
- All extension files begin with `import Foundation`
- After extraction, `LocalChatResponder.swift` must be under 400 lines
- The `respond()` method's selection logic (random pick, index math) stays in the main file

IMPORTANT: Read the actual `// MARK:` section comments in the file to get the real category names. Use those to determine the correct extension file for each pool.
  </action>
  <verify>
    <automated>wc -l "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/LocalChatResponder.swift" && ls "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/LocalChatResponder+"*.swift</automated>
    <manual>LocalChatResponder.swift line count is under 400. Six extension files exist. respond() method still present in the main file.</manual>
  </verify>
  <done>LocalChatResponder.swift is under 400 lines containing only respond() and helpers. Six extension files exist with response pool arrays. Intent matching logic is unchanged.</done>
</task>

</tasks>

<verification>
wc -l Claudy/Claudy/ChatView.swift shows under 200 lines.
wc -l Claudy/Claudy/LocalChatResponder.swift shows under 400 lines.
ls Claudy/Claudy/Chat*.swift shows 4 files (ChatView + 3 sub-views).
ls Claudy/Claudy/LocalChatResponder+*.swift shows 6 files.
</verification>

<success_criteria>
- ChatView.swift under 200 lines — scroll container, ViewModel reference, sub-view composition only
- ChatMessageBubble.swift, ChatInputBar.swift, ChatHeaderBar.swift created with extracted content
- LocalChatResponder.swift under 400 lines — respond() matcher and helpers only
- 6 extension files created with response pool arrays grouped by category
- No behaviour change — chat responds identically, all message types render correctly
</success_criteria>

<output>
After completion, create `/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
