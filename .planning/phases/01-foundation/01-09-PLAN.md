---
phase: 01-foundation
plan: 09
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-05
  - 01-06
  - 01-07
  - 01-08
files_modified:
  - Claudy/Claudy.xcodeproj/project.pbxproj
autonomous: false
requirements:
  - DEBT-14

must_haves:
  truths:
    - "project.pbxproj contains SWIFT_STRICT_CONCURRENCY = complete in the target build settings"
    - "App builds with zero strict concurrency errors AND zero warnings"
    - "All concurrency warnings surfaced by enabling strict mode are resolved file-by-file before Phase 1 is marked complete"
  artifacts:
    - path: "Claudy/Claudy.xcodeproj/project.pbxproj"
      provides: "SWIFT_STRICT_CONCURRENCY = complete in build settings"
      contains: "SWIFT_STRICT_CONCURRENCY = complete"
  key_links:
    - from: "project.pbxproj SWIFT_STRICT_CONCURRENCY = complete"
      to: "App build"
      via: "Xcode build setting applied to Swift compiler"
      pattern: "SWIFT_STRICT_CONCURRENCY = complete"
---

<objective>
Enable SWIFT_STRICT_CONCURRENCY = complete in the Xcode project build settings, verify the build, and resolve any resulting concurrency errors or warnings to achieve a fully clean build with zero errors and zero warnings.

Purpose: This is the final step in Phase 1 — the hard gate for all v3.0 work. It must run last because the timer/GCD fixes (Plan 02), CharacterGeometry (Plan 05), view extractions (Plans 03, 04, 06), DefaultsKeys (Plan 07), and DemoManager merge (Plan 08) all need to be complete first to minimize the warning surface.
Output: project.pbxproj updated; app builds clean under strict concurrency with zero errors and zero warnings.
</objective>

<execution_context>
@/Users/eali/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/PROJECT.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/STATE.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-02-SUMMARY.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-05-SUMMARY.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SWIFT_STRICT_CONCURRENCY = complete to project.pbxproj</name>
  <files>Claudy/Claudy.xcodeproj/project.pbxproj</files>
  <action>
Read project.pbxproj and locate the build settings sections. The file will have `XCBuildConfiguration` entries for both Debug and Release configurations.

Add `SWIFT_STRICT_CONCURRENCY = complete;` to both the Debug and Release build configurations for the `Claudy` target (not the project level — add to the target's build settings).

To find the right location, look for the `XCBuildConfiguration` block that contains `PRODUCT_NAME = Claudy` or the target-specific block. In `.pbxproj` format, this looks like:

```
SWIFT_STRICT_CONCURRENCY = complete;
```

Add this line in the build settings dictionary of both Debug and Release configurations, near other `SWIFT_*` settings if they exist.

After editing, verify with grep:
```bash
grep -n "SWIFT_STRICT_CONCURRENCY" project.pbxproj
```

Should return 2 matches (one Debug, one Release).

IMPORTANT: project.pbxproj is whitespace-sensitive. Match the indentation style of surrounding lines exactly. Do not add or remove trailing semicolons from other settings.
  </action>
  <verify>
    <automated>grep -c "SWIFT_STRICT_CONCURRENCY = complete" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy.xcodeproj/project.pbxproj"</automated>
    <manual>Output is 2 (one per build configuration). File is valid XML/JSON — not corrupted.</manual>
  </verify>
  <done>SWIFT_STRICT_CONCURRENCY = complete present in both Debug and Release build configurations in project.pbxproj.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>SWIFT_STRICT_CONCURRENCY = complete has been added to project.pbxproj. All prior plans (timer fixes, view extractions, DefaultsKeys, DemoManager merge) are complete. The project should now build cleanly under strict concurrency.</what-built>
  <how-to-verify>
1. Open the project in Xcode (or use XcodeBuildMCP to build)
2. Build the project: Product → Build (Cmd+B)
3. Check the Issue Navigator for any errors OR warnings tagged with [Swift Concurrency]
4. Expected: Zero errors AND zero warnings — both are required for Phase 1 to be complete
5. If there are BUILD ERRORS or WARNINGS: Note the exact messages (file, line, error/warning text) and type "errors: [paste all errors and warnings]" so Task 3 can resolve them
6. If the build is fully clean (zero errors, zero warnings): Type "approved"

Note: Unlike earlier phases, warnings are NOT acceptable at this phase gate. Phase 1 requires a completely clean strict concurrency build.
  </how-to-verify>
  <resume-signal>Type "approved" if build is fully clean (zero errors AND zero warnings), or "errors: [error and warning text]" if anything surfaces</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Resolve all concurrency errors and warnings reported by checkpoint</name>
  <files>
    Claudy/Claudy.xcodeproj/project.pbxproj
  </files>
  <action>
This task runs only if the checkpoint reported errors or warnings. Read the error/warning list from the checkpoint output and fix each issue file-by-file.

**Categories of concurrency issues and their fixes (from RESEARCH.md Pattern 8):**

**Category 1 — Non-Sendable types crossing actor boundaries:**
```swift
// Warning: Passing argument of non-sendable type X to @MainActor-isolated function
// Fix: Add Sendable conformance to the type (if it's a simple value type)
// OR mark the crossing point with @Sendable
// OR restructure to avoid the crossing
```

**Category 2 — Stored properties of @MainActor types accessed from non-isolated contexts:**
```swift
// Warning: Main actor-isolated property X can not be referenced from a non-isolated context
// Fix: Ensure the callsite is also @MainActor
// OR use await/async to hop to the main actor
// OR mark the property nonisolated if it is truly safe to access from any context
```

**Category 3 — @State in View structs with isolated access:**
```swift
// Warning: Mutation of captured var X in concurrently-executing code
// Fix: Use Task { @MainActor in ... } to ensure mutation is on main actor
// See RESEARCH.md Pattern 1 (GCD→Task migration) for the exact pattern
```

**Category 4 — Timer/DispatchQueue remnants (should already be fixed by Plans 02 and 07):**
If any Timer or DispatchQueue.main.asyncAfter warnings appear, apply the same Task.sleep migration from RESEARCH.md Pattern 1 and 2.

**Process:**
1. Read each file mentioned in the warnings
2. Apply the appropriate fix for each warning category
3. Write the corrected file
4. After all fixes, trigger a rebuild (via XcodeBuildMCP or instruct user to re-build)
5. Confirm the issue count is reduced — repeat until zero

After all fixes, re-run the checkpoint verification to confirm a clean build.
  </action>
  <verify>
    <automated>grep -c "SWIFT_STRICT_CONCURRENCY = complete" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy.xcodeproj/project.pbxproj"</automated>
    <manual>Build in Xcode again after all fixes. Issue Navigator must show zero errors AND zero warnings. Phase 1 gate is satisfied only when the build is completely clean.</manual>
  </verify>
  <done>Build produces zero errors and zero warnings under SWIFT_STRICT_CONCURRENCY = complete. All concurrency issues resolved. Phase 1 Foundation gate satisfied.</done>
</task>

</tasks>

<verification>
grep -c "SWIFT_STRICT_CONCURRENCY = complete" Claudy/Claudy.xcodeproj/project.pbxproj returns 2.
App builds with zero errors AND zero warnings (confirmed by human checkpoint above).
</verification>

<success_criteria>
- SWIFT_STRICT_CONCURRENCY = complete present in both Debug and Release build configurations
- App builds with zero strict concurrency ERRORS and zero WARNINGS
- Phase 1 Foundation gate is satisfied — codebase is safe to build on
</success_criteria>

<output>
After completion, create `/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-09-SUMMARY.md`

The SUMMARY must include:
- Confirmation that SWIFT_STRICT_CONCURRENCY = complete is set
- Confirmation that the build is fully clean (zero errors, zero warnings)
- List of any concurrency issues that were encountered and how they were resolved
- Final statement: "Phase 1 Foundation complete — all 16 DEBT requirements addressed"
</output>
