---
phase: 01-foundation
plan: 07
type: execute
wave: 2
depends_on:
  - 01-03
  - 01-04
files_modified:
  - Claudy/Claudy/DefaultsKeys.swift
  - Claudy/Claudy/SettingsView.swift
  - Claudy/Claudy/GeneralSettingsSection.swift
  - Claudy/Claudy/AppearanceSettingsSection.swift
  - Claudy/Claudy/ProviderSettingsSection.swift
  - Claudy/Claudy/SoundSettingsSection.swift
  - Claudy/Claudy/FocusTimerSettingsSection.swift
  - Claudy/Claudy/BehaviorModeSettingsSection.swift
  - Claudy/Claudy/QuickLaunchSettingsSection.swift
  - Claudy/Claudy/CharacterViewModel.swift
  - Claudy/Claudy/ChatViewModel.swift
  - Claudy/Claudy/GlobalHotkeyManager.swift
  - Claudy/Claudy/FocusStatsManager.swift
  - Claudy/Claudy/ScratchpadManager.swift
  - Claudy/Claudy/AlarmReminderManager.swift
  - Claudy/Claudy/DailyWrapUpManager.swift
  - Claudy/Claudy/PomodoroManager.swift
  - Claudy/Claudy/StreakManager.swift
  - Claudy/Claudy/CharacterRootView.swift
autonomous: true
requirements:
  - DEBT-12

must_haves:
  truths:
    - "DefaultsKeys.swift exists with a static enum containing every UserDefaults key used in the codebase"
    - "Zero raw string literals remain for UserDefaults keys in any Swift file — all go through DefaultsKeys.*"
    - "All @AppStorage calls use DefaultsKeys.* constants — same string values, just referenced by name"
    - "No UserDefaults preference value changes — keys are identical strings, just referenced via enum"
  artifacts:
    - path: "Claudy/Claudy/DefaultsKeys.swift"
      provides: "Central enum of all UserDefaults keys"
      contains: "enum DefaultsKeys"
    - path: "Claudy/Claudy/SettingsView.swift"
      provides: "@AppStorage(DefaultsKeys.*) declarations"
      contains: "DefaultsKeys."
  key_links:
    - from: "Any file with @AppStorage"
      to: "DefaultsKeys.swift"
      via: "@AppStorage(DefaultsKeys.chatFontSize) — string value unchanged"
      pattern: "AppStorage\\(DefaultsKeys\\."
    - from: "Any file with UserDefaults.standard.set/bool/string"
      to: "DefaultsKeys.swift"
      via: "UserDefaults.standard.set(value, forKey: DefaultsKeys.*)"
      pattern: "forKey: DefaultsKeys\\."
---

<objective>
Create a DefaultsKeys enum centralising all UserDefaults key strings, then migrate all @AppStorage and UserDefaults.standard calls to reference it.

Purpose: 35+ raw string literals across 12 files create typo risk and make key inventory impossible. A centralised enum provides autocomplete, typo safety, and a single source of truth. This runs after Plans 03 and 04 so the extracted section files are already in place and can be migrated cleanly.
Output: DefaultsKeys.swift created; all UserDefaults string literals replaced with enum references.
</objective>

<execution_context>
@/Users/eali/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/PROJECT.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/STATE.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-03-SUMMARY.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DefaultsKeys.swift with complete key inventory</name>
  <files>Claudy/Claudy/DefaultsKeys.swift</files>
  <action>
Before creating the file, run a complete grep across all Swift files to find every UserDefaults key:

```bash
grep -rn '@AppStorage("\|UserDefaults.standard.*forKey: "\|UserDefaults.standard.*forKey:"\|\.set(.*forKey: "\|\.bool(forKey: "\|\.string(forKey: "\|\.data(forKey: "\|\.array(forKey: "\|\.double(forKey: "\|\.integer(forKey: "' /path/to/Claudy/Claudy/ --include="*.swift"
```

Collect every unique string. The research document inventories these keys but may be incomplete:

**Known @AppStorage keys:**
"ChatFontSize", "CharacterOpacity", "ChatWindowOpacity", "UserBubbleColor", "SelectedProvider", "SelectedModel", "UseComplexModel", "SoundEffectsEnabled", "SoundVolume", "CharacterVoiceEnabled", "PomodoroPreset", "PomodoroCustomMinutes", "TimerBadgeScale", "ChattinessLevel"

**Known UserDefaults.standard keys:**
"onboardingComplete", "FirstLaunchDate", "GlobalHotkeyEnabled", "keychainExplainerShown", "chatMode", "CharacterSizePreset", "CharacterWindowOrigin", "ClaudyChatHeight", "FocusStats", "ScratchpadNotes", "AlarmReminderItems", "QuickLaunchShortcuts", "PomodoroPreset", "PomodoroCustomMinutes", "WrapUpHour", "LastWrapUpDate", "IsMuted", "StreakDates", "StreakShownDate"

Add any additional keys found by the grep. Then create DefaultsKeys.swift:

```swift
import Foundation

/// Central registry of all UserDefaults keys used in Claud-y.
/// Use these constants in @AppStorage and UserDefaults.standard calls.
/// IMPORTANT: String values must match exactly — changing a string value
/// will silently reset that preference for existing users.
enum DefaultsKeys {
    // Chat
    static let chatFontSize             = "ChatFontSize"
    static let chatWindowOpacity        = "ChatWindowOpacity"
    static let userBubbleColor          = "UserBubbleColor"
    static let chatMode                 = "chatMode"
    static let claudyChatHeight         = "ClaudyChatHeight"

    // Appearance
    static let characterOpacity         = "CharacterOpacity"
    static let characterSizePreset      = "CharacterSizePreset"
    static let characterWindowOrigin    = "CharacterWindowOrigin"

    // Provider / Model
    static let selectedProvider         = "SelectedProvider"
    static let selectedModel            = "SelectedModel"
    static let useComplexModel          = "UseComplexModel"

    // Sound
    static let soundEffectsEnabled      = "SoundEffectsEnabled"
    static let soundVolume              = "SoundVolume"
    static let characterVoiceEnabled    = "CharacterVoiceEnabled"
    static let isMuted                  = "IsMuted"

    // Behaviour / Personality
    static let chattinessLevel          = "ChattinessLevel"

    // Pomodoro / Focus
    static let pomodoroPreset           = "PomodoroPreset"
    static let pomodoroCustomMinutes    = "PomodoroCustomMinutes"
    static let timerBadgeScale          = "TimerBadgeScale"
    static let focusStats               = "FocusStats"
    static let streakDates              = "StreakDates"
    static let streakShownDate          = "StreakShownDate"

    // Onboarding / First launch
    static let onboardingComplete       = "onboardingComplete"
    static let firstLaunchDate          = "FirstLaunchDate"
    static let keychainExplainerShown   = "keychainExplainerShown"

    // Global hotkey
    static let globalHotkeyEnabled      = "GlobalHotkeyEnabled"

    // Scratchpad / Notes
    static let scratchpadNotes          = "ScratchpadNotes"

    // Alarms
    static let alarmReminderItems       = "AlarmReminderItems"

    // Quick launch
    static let quickLaunchShortcuts     = "QuickLaunchShortcuts"

    // Daily wrap-up
    static let wrapUpHour               = "WrapUpHour"
    static let lastWrapUpDate           = "LastWrapUpDate"
}
```

Add any keys found by the grep that are not in the list above. NEVER change the string values — only wrap them in named constants.
  </action>
  <verify>
    <automated>grep -c "static let" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/DefaultsKeys.swift"</automated>
    <manual>Count is 25 or more. Open the file and verify string values match the original raw strings exactly (spot check 5 keys).</manual>
  </verify>
  <done>DefaultsKeys.swift created with 25+ static let constants. String values are exact matches to original raw strings.</done>
</task>

<task type="auto">
  <name>Task 2a: Migrate @AppStorage and UserDefaults calls in settings section files</name>
  <files>
    Claudy/Claudy/SettingsView.swift
    Claudy/Claudy/GeneralSettingsSection.swift
    Claudy/Claudy/AppearanceSettingsSection.swift
    Claudy/Claudy/ProviderSettingsSection.swift
    Claudy/Claudy/SoundSettingsSection.swift
    Claudy/Claudy/FocusTimerSettingsSection.swift
    Claudy/Claudy/BehaviorModeSettingsSection.swift
    Claudy/Claudy/QuickLaunchSettingsSection.swift
    Claudy/Claudy/CharacterRootView.swift
  </files>
  <action>
In each file listed, replace every raw string UserDefaults key with the corresponding DefaultsKeys.* constant.

**Replacement patterns:**

@AppStorage:
```swift
// Before
@AppStorage("ChatFontSize") private var chatFontSize: Double = 14

// After
@AppStorage(DefaultsKeys.chatFontSize) private var chatFontSize: Double = 14
```

UserDefaults.standard:
```swift
// Before
UserDefaults.standard.set(true, forKey: "onboardingComplete")
UserDefaults.standard.bool(forKey: "onboardingComplete")

// After
UserDefaults.standard.set(true, forKey: DefaultsKeys.onboardingComplete)
UserDefaults.standard.bool(forKey: DefaultsKeys.onboardingComplete)
```

**Process for each file:**
1. Read the file
2. Find all raw string keys matching known DefaultsKeys entries
3. Replace each with the DefaultsKeys.* reference
4. Write the file back

**Verification after each file:** Confirm no raw strings remain for keys in the DefaultsKeys enum. If a key is found that is NOT in DefaultsKeys, add it to DefaultsKeys.swift first.

After completing all nine files, run a verification grep:
```bash
grep -rn '@AppStorage("' Claudy/Claudy/SettingsView.swift Claudy/Claudy/GeneralSettingsSection.swift Claudy/Claudy/AppearanceSettingsSection.swift Claudy/Claudy/ProviderSettingsSection.swift Claudy/Claudy/SoundSettingsSection.swift Claudy/Claudy/FocusTimerSettingsSection.swift Claudy/Claudy/BehaviorModeSettingsSection.swift Claudy/Claudy/QuickLaunchSettingsSection.swift Claudy/Claudy/CharacterRootView.swift
grep -rn 'forKey: "' Claudy/Claudy/SettingsView.swift Claudy/Claudy/GeneralSettingsSection.swift Claudy/Claudy/AppearanceSettingsSection.swift Claudy/Claudy/ProviderSettingsSection.swift Claudy/Claudy/SoundSettingsSection.swift Claudy/Claudy/FocusTimerSettingsSection.swift Claudy/Claudy/BehaviorModeSettingsSection.swift Claudy/Claudy/QuickLaunchSettingsSection.swift Claudy/Claudy/CharacterRootView.swift
```

Both greps must return zero results.
  </action>
  <verify>
    <automated>grep -rn '@AppStorage("' "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/SettingsView.swift" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/GeneralSettingsSection.swift" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/AppearanceSettingsSection.swift" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/ProviderSettingsSection.swift" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/SoundSettingsSection.swift" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/FocusTimerSettingsSection.swift" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/BehaviorModeSettingsSection.swift" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/QuickLaunchSettingsSection.swift" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/CharacterRootView.swift" | wc -l</automated>
    <manual>Output should be 0 for both @AppStorage and forKey: raw string patterns across these nine files.</manual>
  </verify>
  <done>Zero @AppStorage("raw string") patterns remain in the nine settings and root view files. All UserDefaults.standard forKey: calls use DefaultsKeys.* references in these files.</done>
</task>

<task type="auto">
  <name>Task 2b: Migrate @AppStorage and UserDefaults calls in manager files</name>
  <files>
    Claudy/Claudy/CharacterViewModel.swift
    Claudy/Claudy/ChatViewModel.swift
    Claudy/Claudy/GlobalHotkeyManager.swift
    Claudy/Claudy/FocusStatsManager.swift
    Claudy/Claudy/ScratchpadManager.swift
    Claudy/Claudy/AlarmReminderManager.swift
    Claudy/Claudy/DailyWrapUpManager.swift
    Claudy/Claudy/PomodoroManager.swift
    Claudy/Claudy/StreakManager.swift
  </files>
  <action>
In each file listed, replace every raw string UserDefaults key with the corresponding DefaultsKeys.* constant. Follow the same replacement patterns as Task 2a.

**Process for each file:**
1. Read the file
2. Find all raw string keys matching known DefaultsKeys entries
3. Replace each with the DefaultsKeys.* reference
4. Write the file back

If a key is found that is NOT in DefaultsKeys, add it to DefaultsKeys.swift first.

After completing all nine manager files, run a final codebase-wide verification grep to catch any remaining raw strings in any Swift file (not just those listed):
```bash
grep -rn '@AppStorage("' Claudy/Claudy/ --include="*.swift"
grep -rn 'forKey: "' Claudy/Claudy/ --include="*.swift"
```

If the grep still finds raw strings in files not yet migrated, fix those files too.

Note: Keychain `forKey:` strings in KeychainService.swift are intentionally NOT changed — Keychain keys are separate from UserDefaults keys.
  </action>
  <verify>
    <automated>grep -rn '@AppStorage("' "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/" --include="*.swift" | wc -l</automated>
    <manual>Output should be 0. Also run: grep -rn 'forKey: "' Claudy/Claudy/ --include="*.swift" and confirm any remaining hits are NOT UserDefaults keys (they may be Keychain keys in KeychainService.swift — those are correct and should NOT be changed).</manual>
  </verify>
  <done>Zero @AppStorage("raw string") patterns remain in any Swift file. All UserDefaults.standard forKey: calls use DefaultsKeys.* references. Keychain forKey: strings in KeychainService.swift are intentionally left as-is (Keychain != UserDefaults).</done>
</task>

</tasks>

<verification>
grep -rn '@AppStorage("' Claudy/Claudy/ --include="*.swift" returns 0 results.
grep -c "static let" Claudy/Claudy/DefaultsKeys.swift returns 25+.
</verification>

<success_criteria>
- DefaultsKeys.swift exists with 25+ static let constants
- Zero @AppStorage("raw string") patterns in any Swift file
- Zero UserDefaults.standard forKey: "raw string" patterns (Keychain strings in KeychainService.swift are exempt)
- All string values in DefaultsKeys match the originals exactly — no preference resets
</success_criteria>

<output>
After completion, create `/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-07-SUMMARY.md`
</output>
