---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Claudy/Claudy/KeychainService.swift
  - Claudy/Claudy/ClaudeAPIService.swift
  - Claudy/Claudy/IdleMonitor.swift
  - Claudy/Claudy/ChatViewModel.swift
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02
  - DEBT-03
  - DEBT-04
  - DEBT-05
  - DEBT-06

must_haves:
  truths:
    - "DEBT-01 through DEBT-06 are confirmed fixed in source and marked complete in REQUIREMENTS.md"
    - "Every fix is verified by reading the specific file+line documented in research"
    - "No source files are modified — this is read-only verification"
  artifacts:
    - path: "Claudy/Claudy/KeychainService.swift"
      provides: "SEC-01 fix — kSecAttrAccessibleWhenUnlockedThisDeviceOnly on all write paths"
      contains: "kSecAttrAccessibleWhenUnlockedThisDeviceOnly"
    - path: "Claudy/Claudy/ClaudeAPIService.swift"
      provides: "BUG-02 fix — claude-opus-4-6 hard-coded for complex model"
      contains: "claude-opus-4-6"
    - path: "Claudy/Claudy/IdleMonitor.swift"
      provides: "BUG-03 fix (idle_5min guard) and BUG-04 fix (afternoon hours)"
      contains: "BUG-03 fix"
    - path: "Claudy/Claudy/ChatViewModel.swift"
      provides: "BUG-05 fix — cancel() before messages = [] in clearHistory()"
      contains: "cancel()"
  key_links:
    - from: "REQUIREMENTS.md"
      to: "Source files"
      via: "Read verification of each fix line"
      pattern: "kSecAttrAccessibleWhenUnlockedThisDeviceOnly|BUG-0[3-4] fix|claude-opus-4-6"
---

<objective>
Verify that DEBT-01 through DEBT-06 (BUG-01 through BUG-05 and SEC-01) are already fixed in the current codebase.

Purpose: Research confirmed these fixes are committed. This plan closes them in the requirements tracking without making any code changes.
Output: Confirmed verification of six requirements — no files modified.
</objective>

<execution_context>
@/Users/eali/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/PROJECT.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/ROADMAP.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/STATE.md
@/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify DEBT-01, DEBT-02, DEBT-06 (model IDs + Keychain security)</name>
  <files>
    Claudy/Claudy/KeychainService.swift
    Claudy/Claudy/ClaudeAPIService.swift
    Claudy/Claudy/SettingsView.swift
  </files>
  <action>
Read each file at the documented lines and confirm:

1. **DEBT-01 (BUG-01):** `KeychainService.swift` line 83 — `case .claude: return "claude-3-5-haiku-20241022"` must be present. This confirms the fast model ID is correct.

2. **DEBT-02 (BUG-02):** `ClaudeAPIService.swift` line 26 — `return useComplex ? "claude-opus-4-6"` must be present. `SettingsView.swift` — confirm `"claude-haiku-4-5-20251001"` appears in the picker options. This confirms both smart model sync points are correct.

3. **DEBT-06 (SEC-01):** `KeychainService.swift` lines 127 and 179 — `kSecAttrAccessible: kSecAttrAccessibleWhenUnlockedThisDeviceOnly` must appear on all write paths. Confirm it is present at both lines.

Do NOT modify any files. This is read-only verification. If any fix is missing (not found at the documented line), note the discrepancy in the SUMMARY but do not attempt to fix it in this plan — that would be a separate task.
  </action>
  <verify>
    <automated>grep -n "claude-3-5-haiku-20241022" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/KeychainService.swift" && grep -n "claude-opus-4-6" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/ClaudeAPIService.swift" && grep -n "kSecAttrAccessibleWhenUnlockedThisDeviceOnly" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/KeychainService.swift"</automated>
    <manual>All three greps return matches. No files were modified.</manual>
  </verify>
  <done>DEBT-01, DEBT-02, DEBT-06 confirmed present in source. Exact line numbers recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Verify DEBT-03, DEBT-04, DEBT-05 (idle monitor + crash fix)</name>
  <files>
    Claudy/Claudy/IdleMonitor.swift
    Claudy/Claudy/ChatViewModel.swift
  </files>
  <action>
Read each file at the documented lines and confirm:

1. **DEBT-03 (BUG-03):** `IdleMonitor.swift` line 83 — look for `// BUG-03 fix` comment AND a guard checking `vm.animationState != .drowsy` before the idle bubble fires. Both the guard and the comment must be present.

2. **DEBT-04 (BUG-04):** `IdleMonitor.swift` line 370 — `case 12...21: return .afternoon // BUG-04 fix` must be present. This confirms hours 18–21 are mapped to `.afternoon` and not falling through to `.launch`.

3. **DEBT-05 (BUG-05):** `ChatViewModel.swift` around lines 174–178 — `clearHistory()` must call `cancel()` before setting `messages = []`. Confirm the call order: `cancel()` first, then `messages = []`.

Do NOT modify any files. Read-only verification only. Note any discrepancies in the SUMMARY.
  </action>
  <verify>
    <automated>grep -n "BUG-03 fix" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/IdleMonitor.swift" && grep -n "BUG-04 fix" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/IdleMonitor.swift" && grep -n "cancel()" "/Users/eali/Documents/App Dev/Apps/Claud-y/Claudy/Claudy/ChatViewModel.swift"</automated>
    <manual>All three greps return matches. clearHistory() structure confirmed: cancel() before messages = [].</manual>
  </verify>
  <done>DEBT-03, DEBT-04, DEBT-05 confirmed present in source. All six DEBT-01–06 requirements verified as already fixed.</done>
</task>

</tasks>

<verification>
All six DEBT-01 through DEBT-06 requirements confirmed fixed via grep verification of exact lines documented in research. Zero files modified.
</verification>

<success_criteria>
- DEBT-01: `claude-3-5-haiku-20241022` found in KeychainService.swift
- DEBT-02: `claude-opus-4-6` found in ClaudeAPIService.swift
- DEBT-03: `BUG-03 fix` comment found in IdleMonitor.swift with correct guard
- DEBT-04: `BUG-04 fix` comment found in IdleMonitor.swift with `case 12...21: return .afternoon`
- DEBT-05: `cancel()` called before `messages = []` in ChatViewModel.clearHistory()
- DEBT-06: `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` found on both write paths in KeychainService.swift
</success_criteria>

<output>
After completion, create `/Users/eali/Documents/App Dev/Apps/Claud-y/.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
